# Tools for Domain Scheduler Agent

"""
1. check availability of a provider for a given date and time / date range - args (provider_id, name, date and time range)
    - availability rules
    - existing appointments
    - blocked/availability exceptions
2. booking an appointment - args (provider_id, provider_name, user_id, user, service_id, service_name, date and time)
3. my appointments - args (user_id, provider_id)
4. what services are available for a provider - args (provider_id)
5. what providers are available for a service - args (service_id)
6. get provider details - args (provider_id)
7. get service details - args (service_id)
8. get user details - args (user_id)
9. get appointment details - args (appointment_id)
"""

